"""The application version — the single source of truth.

.claude/bump.json rewrites this file. Anything that displays the version
imports __version__ from here rather than repeating the literal, so a bump
cannot leave one copy behind.
"""

__version__ = "1.0.0"
