"""Slipcase - Convert 2D game cover art into realistic 3D boxart renders.

The version lives in core/version.py, which api/ and ui/ can import. This
directory is a sys.path entry rather than an importable package, so a
constant defined here would be unreachable from the code that shows it.
"""
