{
  "$comment": "Recipe consumed by ~/.claude/skills/bump/SKILL.md. Source of truth is __version__ in core/version.py, which ui/ imports so the About dialog cannot go stale. Test suite is the gate: post_check runs pytest.",
  "version_source": "core/version.py",
  "version_pattern": "__version__\\s*=\\s*\"([0-9]+\\.[0-9]+\\.[0-9]+)\"",
  "files": [
    {
      "path": "core/version.py",
      "pattern": "__version__ = \"{OLD}\"",
      "replace": "__version__ = \"{NEW}\""
    }
  ],
  "post_check": "python3 -m pytest tests/ -v --tb=short",
  "todos": [
    "Add a `## [{NEW}] — {TODAY}` section to CHANGELOG.md, in the [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) format the file already uses. Per the project's STANDARDS.md `All tests must pass before any commit`, the post_check pytest gate has already run.",
    "Tag the release: `git tag v{NEW}`."
  ]
}
